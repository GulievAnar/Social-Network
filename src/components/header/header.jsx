import React from 'react';
import styles from './header.module.css';

const Header = () => {
    return <div className={styles.blockHeader}>
        <h1>{"Header"}</h1>
    </div>
}

export default Header; 